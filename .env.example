# Wird von docker-compose.yml automatisch geladen, um den PostgreSQL-Service
# zu konfigurieren. Optional - ohne diese Datei greifen die im
# docker-compose.yml hinterlegten Standardwerte.
#
# cp .env.example .env

POSTGRES_USER=app_user
POSTGRES_PASSWORD=app_password
POSTGRES_DB=application_manager
